victoria-metrics-agent:
  config:
    remoteWrite:
      - url: "http://vm-single-server.monitoring.svc.cluster.local:8428/api/v1/write"
    global:
      scrape_interval: 30s
  rbac:
    create: true
