apiVersion: v1
kind: Service
metadata:
  name: audiomuse
  namespace: audiomuse
  labels:
    app.kubernetes.io/name: audiomuse
    app.kubernetes.io/component: flask
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      protocol: TCP
  selector:
    app.kubernetes.io/name: audiomuse
    app.kubernetes.io/component: flask
