apiVersion: apps/v1
kind: Deployment
metadata:
  name: audiomuse-redis
  namespace: audiomuse
  labels:
    app.kubernetes.io/name: audiomuse-redis
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: audiomuse-redis
  template:
    metadata:
      labels:
        app.kubernetes.io/name: audiomuse-redis
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 999
        runAsGroup: 999
        fsGroup: 999
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: redis
          image: redis:8-alpine@sha256:978f0e01593e65eed801f2402944efcd936d43b5027e4908a7897baf88ed6241
          securityContext:
            readOnlyRootFilesystem: true
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
          ports:
            - name: redis
              containerPort: 6379
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 500m
              memory: 256Mi
          volumeMounts:
            - name: redis-data
              mountPath: /data
            - name: tmp-dir
              mountPath: /tmp
      volumes:
        - name: redis-data
          emptyDir: {}
        - name: tmp-dir
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: audiomuse-redis
  namespace: audiomuse
  labels:
    app.kubernetes.io/name: audiomuse-redis
spec:
  ports:
    - name: redis
      port: 6379
      targetPort: 6379
  selector:
    app.kubernetes.io/name: audiomuse-redis
