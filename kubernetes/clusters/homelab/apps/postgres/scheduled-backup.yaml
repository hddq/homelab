apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: postgres-backup-every-6h
  namespace: postgres
spec:
  schedule: "0 0 */6 * * *"
  backupOwnerReference: cluster
  cluster:
    name: postgres
  method: plugin
  pluginConfiguration:
    name: barman-cloud.cloudnative-pg.io
