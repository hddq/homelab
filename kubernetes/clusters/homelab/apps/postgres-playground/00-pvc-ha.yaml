apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-pvc-ha
  namespace: playground
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: openebs-hostpath
  resources:
    requests:
      storage: 2Gi
