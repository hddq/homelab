apiVersion: v1
kind: Service
metadata:
  name: postgres-service
  namespace: playground
  annotations:
    metallb.io/loadBalancerIPs: 192.168.41.16
spec:
  selector:
    app: postgres
  type: LoadBalancer
  externalTrafficPolicy: Local
  ports:
    - port: 5432
      targetPort: 5432
