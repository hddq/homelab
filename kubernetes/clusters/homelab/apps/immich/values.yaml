immich:
  server:
    ingress:
      main:
        enabled: true
        className: infra-traefik
        hosts:
          - host: immich.stg.hddq.org
            paths:
              - path: /
                service:
                  identifier: main
  valkey:
    enabled: true
  controllers:
    main:
      containers:
        main:
          image:
            tag: v3.1.0
          env:
            DB_HOSTNAME: postgres-rw.postgres.svc.cluster.local
            DB_USERNAME: immich
            DB_DATABASE_NAME: immich
            DB_PASSWORD:
              valueFrom:
                secretKeyRef:
                  name: immich-postgres
                  key: password
  immich:
    persistence:
      library:
        existingClaim: "immich-nfs-pvc"
  env:
    TZ: "UTC"
