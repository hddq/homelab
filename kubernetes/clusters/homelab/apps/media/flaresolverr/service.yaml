apiVersion: v1
kind: Service
metadata:
  name: flaresolverr
  labels:
    app.kubernetes.io/name: flaresolverr
spec:
  type: ClusterIP
  ports:
    - port: 8191
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: flaresolverr
