apiVersion: v1
kind: Service
metadata:
  name: qui
  labels:
    app.kubernetes.io/name: qui
spec:
  type: ClusterIP
  ports:
    - port: 7476
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: qui
