apiVersion: v1
kind: Service
metadata:
  name: bindery
  labels:
    app.kubernetes.io/name: bindery
spec:
  type: ClusterIP
  ports:
    - port: 8787
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: bindery
