apiVersion: viaduct.ai/v1
kind: ksops
metadata:
  name: argocd-repo-secret
  annotations:
    config.kubernetes.io/function: |
      exec:
        path: ksops
files:
  - repo-secret.yaml
  - admin-secret.yaml
