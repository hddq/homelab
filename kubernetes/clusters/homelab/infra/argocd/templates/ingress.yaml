apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: argocd-server-ingress
  namespace: argocd
spec:
  ingressClassName: infra-traefik
  rules:
    - host: {{ printf "argocd.%s" .Values.homelab.baseDomain }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: infra-argocd-server
                port:
                  number: 80
