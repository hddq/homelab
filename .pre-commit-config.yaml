repos:
  - repo: https://github.com/gitleaks/gitleaks
    rev: v8.30.1
    hooks:
      - id: gitleaks
  - repo: https://github.com/adrienverge/yamllint.git
    rev: v1.38.0
    hooks:
      - id: yamllint
        args: [-c, .yamllint]
  - repo: https://github.com/shellcheck-py/shellcheck-py
    rev: v0.11.0.1
    hooks:
      - id: shellcheck
        exclude: ^\.envrc$
  - repo: local
    hooks:
      - id: ruff
        name: ruff
        entry: nix run nixpkgs#ruff -- check --fix
        language: system
        types_or: [python, pyi]
        require_serial: true
      - id: ruff-format
        name: ruff-format
        entry: nix run nixpkgs#ruff -- format
        language: system
        types_or: [python, pyi]
        require_serial: true
      - id: alejandra
        name: alejandra
        entry: nix run nixpkgs#alejandra -- .
        language: system
        types: [nix]
      - id: deadnix
        name: deadnix
        entry: nix run nixpkgs#deadnix -- .
        language: system
        types: [nix]
      - id: statix
        name: statix
        entry: nix run nixpkgs#statix -- fix
        language: system
        types: [nix]
