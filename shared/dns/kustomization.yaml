apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
configMapGenerator:
  - name: unbound-config
    files:
      - unbound.conf
      - unbound-k8s.conf
  - name: blocky-config
    files:
      - blocky.yml
