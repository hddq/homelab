apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-pvc-ha
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn-ha
  resources:
    requests:
      storage: 2Gi