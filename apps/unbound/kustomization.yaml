apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: unbound

resources:
  - deployment.yaml
  - service.yaml
  - namespace.yaml

configMapGenerator:
  - name: unbound-config
    files:
      - config/unbound.conf
