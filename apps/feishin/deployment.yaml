apiVersion: apps/v1
kind: Deployment
metadata:
  name: feishin
  namespace: feishin
  labels:
    app.kubernetes.io/name: feishin
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: feishin
  template:
    metadata:
      labels:
        app.kubernetes.io/name: feishin
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
      containers:
        - name: feishin
          image: ghcr.io/jeffvli/feishin:1.15.1@sha256:5c0b3a1f307f8d9508c9fd5dcef04349b6cda1740ae95d478498dc86e3e432bf
          securityContext:
            readOnlyRootFilesystem: true
          ports:
            - name: http
              containerPort: 9180
              protocol: TCP
          readinessProbe:
            httpGet:
              path: /
              port: 9180
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 1
            failureThreshold: 3
          env:
            - name: PUBLIC_PATH
              value: "/"
            - name: SERVER_NAME
              value: "navidrome"
            - name: SERVER_LOCK
              value: "true"
            - name: SERVER_TYPE
              value: "navidrome"
            - name: SERVER_URL
              value: "https://navidrome.lab.hddq.org"
            - name: REMOTE_URL
              value: "https://navidrome.lab.hddq.org"
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
            limits:
              cpu: 200m
              memory: 256Mi
          volumeMounts:
            - name: tmp-volume
              mountPath: /tmp
            - name: conf-volume
              mountPath: /etc/nginx/conf.d
      volumes:
        - name: tmp-volume
          emptyDir: {}
        - name: conf-volume
          emptyDir: {}
